"""Replay engine placeholder."""

from ..exceptions import SpecificationRequiredError


class ReplayEngine:
    """Placeholder for deterministic replay execution."""

    def __init__(self) -> None:
        raise SpecificationRequiredError("ReplayEngine requires the formal specification.")
