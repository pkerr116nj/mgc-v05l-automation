"""Replay data loading placeholder."""

from ..exceptions import SpecificationRequiredError


class ReplayDataLoader:
    """Placeholder for deterministic replay data ingestion."""

    def __init__(self) -> None:
        raise SpecificationRequiredError("ReplayDataLoader requires the formal specification.")
