"""Market data adapter placeholder."""

from ..exceptions import SpecificationRequiredError


class MarketDataAdapter:
    """Placeholder market data adapter."""

    def __init__(self) -> None:
        raise SpecificationRequiredError("MarketDataAdapter requires the remaining implementation details.")
