"""Reconciliation engine placeholder."""

from ..exceptions import SpecificationRequiredError


class ReconciliationEngine:
    """Placeholder reconciliation engine."""

    def __init__(self) -> None:
        raise SpecificationRequiredError("ReconciliationEngine requires broker-state comparison logic.")
