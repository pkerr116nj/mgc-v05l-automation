"""Alerting layer placeholder."""

from ..exceptions import SpecificationRequiredError


class AlertingLayer:
    """Placeholder alerting layer."""

    def __init__(self) -> None:
        raise SpecificationRequiredError("AlertingLayer requires operator notification routing details.")
