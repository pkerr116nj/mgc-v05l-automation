"""Risk engine placeholder."""

from ..exceptions import SpecificationRequiredError


class RiskEngine:
    """Build-stage placeholder for risk gating and position constraints."""

    def __init__(self) -> None:
        raise SpecificationRequiredError("RiskEngine requires the formal specification.")
