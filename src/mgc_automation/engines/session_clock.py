"""Session clock engine placeholder."""

from ..exceptions import SpecificationRequiredError


class SessionClock:
    """Build-stage placeholder for the session clock."""

    def __init__(self) -> None:
        raise SpecificationRequiredError("SessionClock requires the formal specification.")
