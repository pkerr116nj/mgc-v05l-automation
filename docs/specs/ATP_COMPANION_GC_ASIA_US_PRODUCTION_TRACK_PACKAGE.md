# ATP Companion GC Asia+US Production-Track Candidate Package

## Status

This document records the current ATP-derived production-track candidate package.

It is:
- a production-track candidate
- the frozen paper-pilot package for the current GC admission pass
- explicit and provenance-preserving
- not a benchmark promotion
- not a semantic change to the frozen ATP benchmark lane

## Candidate Identity

- lane family: `ATP Companion Candidate v1`
- raw candidate lane label: `ATP Companion Candidate v1 — GC / Asia + US Executable, London Diagnostic-Only`
- replay/study identity: `atp_companion_v1__candidate_gc_asia_us`
- raw paper lane id: `atp_companion_v1_gc_asia_us`
- raw shared strategy identity: `ATP_COMPANION_V1_GC_ASIA_US`
- admitted paper package lane id: `atp_companion_v1_gc_asia_us_production_track`
- admitted paper package shared strategy identity: `ATP_COMPANION_V1_GC_ASIA_US_PRODUCTION_TRACK`
- admitted paper standalone strategy id: `atp_companion_v1__production_track_gc_asia_us`
- admitted paper config: `config/probationary_pattern_engine_paper_atp_companion_v1_gc_asia_us_production_track.yaml`
- symbol: `GC`

## Current Package

Core candidate:
- `GC / Asia + US`

Attached package components:
- narrow `US_LATE` safeguard
  - `2-bar no-traction + adverse excursion`
  - admitted runtime profile: `0.25R` minimum 2-bar traction and `0.65R` adverse threshold
- outer governance
  - halt-only `$3,000`
  - desk flatten threshold intentionally pushed out of scope for this package so the package remains halt-only at the outer desk layer

## Admitted Paper Package Status

This package is now intended to be carried forward as:
- an explicit admitted paper package
- a production-track candidate package
- the frozen paper-pilot package for bounded operator validation
- a lane-local package with explicit runtime overlay and outer governance

It is not:
- the frozen ATP benchmark
- a benchmark replacement
- a repo-wide ATP default

## Interpretation

This package is currently the strongest documented ATP production-track candidate package because it:
- preserves a large share of the GC candidate economics
- materially improves drawdown and worst-episode containment versus raw GC baseline
- keeps additional rule complexity relatively small

This package does **not** mean:
- the frozen ATP benchmark is replaced
- benchmark-local assumptions are invalid everywhere
- governance overlays are equivalent to core strategy improvements
- broader ATP standardization is already justified

## Benchmark Relationship

Reference benchmark:
- `ATP Companion Baseline v1 — Asia + US Executable, London Diagnostic-Only`

Relationship to the benchmark:
- same ATP family provenance
- different explicit lane identity
- different instrument scope
- different candidate package posture
- benchmark replacement still requires explicit promotion

## Promotion Rule

Success as a production-track candidate package does not promote this lane into the benchmark slot.

Benchmark promotion remains a separate explicit decision.
